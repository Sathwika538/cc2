<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>File Upload</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.4.3/dist/css/bootstrap.min.css">
  <style>
    .upload-form {
      max-width: 400px;
      margin: 0 auto;
      margin-top: 50px;
    }
  </style>
     <link href="css/sem_notes.css" rel="stylesheet"/>
</head>

<body>
  <div class="container">
    <div class="row">
      <div class="col-md-6 offset-md-3">
        <div class="upload-form">
          <h4>Maintain the structure of the uploading PDF in the respective format :</h4>
          <ul>
            <li>Notes - Notes_(TopicName)</li>
            <li>Short Notes - ShortNotes_(TopicName)</li>
            <li>Previous Year QuestionPapers/Solutions - PYQ_(TopicName)</li>
            <li>Material - Material_(TopicName)</li>
          </ul>
          <h3>Upload PDF</h3>
          <form method="POST" action="/upload/sem2/COA" enctype="multipart/form-data">
            <input type="file" name="fileToUpload" />
            <button type="submit">Upload</button>
          </form>
          <div id="uploadResult" class="mt-3"></div>
        </div>
      </div>
    </div>
    <div class="search-container mb-4">
      <form action="/notes/sem2/COA" method="get">
        <div class="input-group">
          <input type="text" class="form-control" placeholder="Search files" name="q">
          <button class="btn btn-outline-secondary" type="submit"><i class="fas fa-search"></i></button>
        </div>
      </form>
    </div>
    <h1>Uploaded files</h1>
      
        <div class="row">
          <% if (files && files.length) { %>
            <% files.forEach(function(file) { %>
              <div class="col-sm-6 col-md-4 col-lg-3 mb-4">
                <div class="card h-100">
                  <% if (file.mimetype && file.mimetype.startsWith("image/")) { %>
                    <% generateThumbnail(path.join(uploadsDir, file.filename)).then(function(thumbnailFilename) { %>
                      <img src="/<%= thumbnailFilename %>" class="card-img-top" alt="<%= file.originalname %>">
                    <% }); %>
                  <% } else { %>
                    <div class="text-center pt-3">
                      <i class="fas fa-file fa-5x"></i>
                    </div>
                  <% } %>
                  <div class="card-body">
                    <h5 class="card-title"><%= file.originalname %></h5>
                    <a href="/download/sem2/COA/<%= file.filename %>" class="btn btn-primary">Download</a>
                  </div>
                </div>
              </div>
            <% }); %>
          <% } else { %>
            <p>No files are Uploaded/Matching your search</p>
          <% } %>
        </div>

  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.4.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
